namespace zmb{
    enum struct task_statement_enum{
        test = -666,
        jump,
        loop,
        unknown = 0,
        animate = 1,
        banish,
        disturb,
        forget,
        invoke,
        moan,
        remember,
        say,
    };
    enum struct entity_statement_enum{

    };
    enum struct flow_control_statement_enum{

    };
    enum struct operators_statements_enum{



    };

} //namespace zmb